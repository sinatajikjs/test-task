interface IListElement {
  label: string;
  type: string;
  className: string;
}

interface IElement {
  label: string;
  type: string;
  id: string;
  className: string;
  name: string;
  required: boolean;
  value: string;
}
